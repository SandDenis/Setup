<h1>Category: <%= @category.name %> </h1>

<h5>
  <% if logged_in? && current_user.admin%>
    <button> <%= link_to "Edit category name", edit_category_path(@category) %> </button>
  <% end%>
</h5>

<h3> Articles </h3>

<%= will_paginate @articles %>

<%= render 'articles/article' %>

<%= will_paginate @articles %>