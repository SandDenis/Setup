<!DOCTYPE html>
<html>
  <head>
    <title>Setup</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if logged_in? %>
    <%= link_to current_user.username, user_path(current_user)%>
    <%= link_to 'Log out', logout_path, data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %>  |
    <%= link_to 'Delete profile', user_path(current_user), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>
    <% else %>
     <%= link_to 'Log in', login_path %> |
     <%= link_to "Sign up" ,  signup_path %> |
    <% end %>
    <%= link_to 'Articles listing', articles_path %> |
    <%= link_to 'Users', users_path %> 
    <br/>
    <%= render 'layouts/messages'%>
    <%= yield %>
  </body>
</html>
